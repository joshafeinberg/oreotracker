apply plugin: 'kotlin'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    compile deps.kotlin.stdlib
    compile deps.ktor.gson
    compile "com.bnorm.ktor.retrofit:ktor-retrofit:0.1.1"
}

sourceCompatibility = "7"
targetCompatibility = "7"
